#!/bin/bash

# Run setup.sh and wait for it to complete
./setup.sh

# wait

# Launch rover-start in a new terminal
ros2 launch rover_bringup rover-start.py &

# Launch aruco_tracker in a new terminal
ros2 launch rover_bringup ardu-cam.py &

# Launch aruco_tracker in a new terminal
ros2 launch rover_bringup aruco_tracker.py &
