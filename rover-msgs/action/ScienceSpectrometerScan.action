bool correct_dark_counts false
bool correct_nonlinearity false
uint32 integration_time_ms 0
---
float64[] temperatures

# the wavelengths & intensities are always the same length,
# and every wavelength is paired with the intensity at the corresponding index
float64[] wavelengths
float64[] intensities
---
